'use strict';

var React = require('react/addons');

var WCDataValidator = React.createClass({
    render: function () {
        return (
            <h1>Hello there WCDataValidator!</h1>
        );
    }
});

module.exports = WCDataValidator;
