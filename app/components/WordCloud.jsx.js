'use strict';

var React = require('react/addons');

var WordCloud = React.createClass({    
    render: function () {
        return (
            <h1>Hello there WordCloud!</h1>
        );
    }
});

module.exports = WordCloud;
